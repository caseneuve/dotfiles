
# lenovo local settings:
# =====================
# Last update: 31.05.18, 02:32:26 @lenovo

# exec_always feh --bg-fill ~/.i3/wallpaper/i3wall.jpg start # jeżeli wpis z początku nie działa

# bindsym F1 workspace $ws5; exec mupdf ~/.i3/cheatsheet.pdf 
# for_window [class="MuPDF"] fullscreen enable

bindsym $mod+Shift+Delete       exec shutdown now
bindsym $mod+Shift+Home         exec systemctl hibernate
bindsym $mod+Shift+End          exec --no-startup-id ~/.dotfiles/i3/i3lock/lock.sh; exec systemctl suspend 
bindsym Pause                   exec systemctl suspend

# zmienne dla hangoutów
set $right 990
set $down 505
set $top 10
set $sright 755

#
set $term st

# rofi helper (mod+' nie działa)
bindsym $mod+backslash          exec --no-startup-id ~/.dotfiles/bin/rofi-media.sh

# skype
for_window [class="Skype"] move window to position 1070 20

